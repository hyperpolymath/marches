= FALSIFIERS

Each row names what would kill or narrow the claim, the test, and the
current status. Idiom: *held* (tested, survived), *held-but-untested*
(design answer exists, test not yet run), *open* (no answer yet).

[cols="1,3,3,1"]
|===
|ID |Falsifier |Test / response |Status

|F1
|Prior art already makes region-crossing a typing judgment with
unconstructibility.
|First sweep done 2026-07-07: agda-routing (convergence, deeper),
Kirigami (interfaces, post-hoc SMT), NetKAT (data plane), metarouting
(algebra language) — none make the crossing judgment itself the typed,
unconstructible object. Full systematic sweep owed.
|held-but-untested (at depth)

|F2
|The composite theorem is vacuous: stratification (`ab`) does all the
work and within-region strictness is decorative.
|Discharged 2026-07-07: `Marches/Freewheel.lean` — the identity-extension
algebra is inflationary but not strictly so, and `f2_discharged` exhibits
a well-typed NON-EMPTY returning trace. Drop `hA` and
`wt_cycles_are_empty` is false: strictness is load-bearing.
|held

|F3
|Any deployment story requiring global replacement (flag-day) fails.
|Design answer: attested overlay, RPKI-shaped — announcements carry
typed-policy attestations; incremental adoption. Rungs 4–5.
|held-but-untested

|F4
|Operator practice (MED games, community hacks) is non-monotone and
unrepresentable; the discipline dies on contact with reality.
|Doctrine: incompleteness must cost *re-expression*, not impossibility
(five-lane gateway). Test: express one MED-style hack inside the
discipline or prove it needs quarantine. 
|open

|F5
|The GR strictness gap ((fromCust, c)) silently re-enters and the
kernel theorems get applied where they do not hold.
|`gr_not_strict` is a PROVEN negative; rung 1 done 2026-07-07:
`Marches/GRHierarchy.lean` names the assumption (`Acyclic`) and
`gr_loop_free` consumes it as an explicit hypothesis — threaded,
never silently discharged. The hypothesis itself remains ASSUMED
about real topologies, as in Gao–Rexford 2001.
|held
|===
