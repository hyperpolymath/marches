= STATE — cross-session log

== 2026-07-07 — rung 2 built and verified (Claude Code session)

* Digraph foundation lifted: `Marches/Digraph.lean` — `RPath`,
  `RPath.snoc/trans/flip`, and `Acyclic` moved out of the GR namespace
  into `Marches`. `GRHierarchy.lean` refactored to consume them, so the
  customer→provider hierarchy (rung 1) and the region DAG (rung 2) are
  the SAME `Marches.Acyclic`. GR proofs unchanged in substance.
* Reassembly by composition: `Marches/Reassembly.lean` —
  - `Discipline` abstracts the shared invariant (strict order + typed
    step judgment whose every step worsens); `Discipline.no_loop`
    proves loop-freedom ONCE, generically.
  - `PathAlgebra.toDiscipline` — the rung-0 kernel is an instance;
    `toDiscipline_no_loop` recovers `no_return`.
  - `DiscSys.compose` (HEADLINE): a finite acyclic DAG of disciplines
    is itself a `Discipline`. Composite `btrans` needs only
    `RPath.trans`; composite `birrefl` needs EXACTLY `Acyclic S.E` (the
    honest n-region home of the assumption); `cstep_worsens` is
    discharged from the componentwise obligations + DAG stratification,
    no global re-proof — the by-construction counterpart of Kirigami
    cut soundness. Because `compose` returns a `Discipline`, composites
    NEST: `Reassembly.nestedComposed` composes a composite as a region.
  - Negatives: `no_foreign_local` (a region's label can't fire from
    another region), `no_self_cross` (acyclicity ⇒ self-edge
    unconstructible).
  - Crossing maps `τ` are UNCONSTRAINED — worsening is by DAG position.
* `Interface.strict` RETIRED (roadmap-sanctioned): rung 2 confirmed
  order-preservation of crossing maps is never load-bearing, so the
  field was deleted from `Compose.Interface` (and its provider in
  `Freewheel.lean`) rather than kept as a proven-unused wart.
* Gates: `lake build` → `Build completed successfully.` (14 modules,
  zero deps); `grep -rnw sorry` empty; `#eval orbit` unchanged
  (`[(false, false), (true, true), (false, false), (true, true)]`).
* Claim discipline held: loop-freedom of the typed judgment is NOT
  protocol convergence (agda-routing's ground); no convergence claim
  added anywhere.

== 2026-07-07 — rung 1 built and verified (Claude Code session)

* GR wired into Trace: `Marches/GRTrace.lean` — the five legal
  (relationship, signature) moves are the constructors of `GStep`;
  ill-typed moves (valley, second peer hop, any move from ⊥) are
  unconstructible. Derivations from `c` are EXACTLY the valley-free
  words (`gtrace_iff_valley_free`), and coincide with non-⊥ evaluation
  of the rung-0 table (`valley_free_iff_gwalk`).
* Acyclicity explicit: `Marches/GRHierarchy.lean` — `Acyclic T.custOf`
  is a named hypothesis; `gr_loop_free` derives (node, signature)
  state-level loop-freedom UNDER it. Never silently discharged (F5).
  Convergence remains agda-routing's ground; not claimed anywhere.
* F2 discharged: `Marches/Freewheel.lean` — identity-extension algebra,
  inflationary but not strict; `f2_discharged` exhibits a well-typed
  non-empty returning trace. `hA`/`hB` are load-bearing; stratification
  alone is not enough. FALSIFIERS F2 → held.
* Gates re-run: `lake build` → `Build completed successfully.`;
  `grep -rnw sorry` empty; `#eval orbit` unchanged
  (`[(false, false), (true, true), (false, false), (true, true)]`).
* Deferred to rung 2 (unchanged): n-region DAG composition;
  `Interface.strict` becomes load-bearing or is deleted.

== 2026-07-07 — rung 0 built and verified (claude.ai session)

* Repo scaffolded; six modules + root compile clean under Lean 4
  v4.15.0, zero dependencies, zero `sorry`.
* `#eval orbit` output (machine-evaluated):
  `[(false, false), (true, true), (false, false), (true, true)]`.
* Prior-art first sweep done; agda-routing and Kirigami found and
  written into LIMITATIONS; claim narrowed accordingly *before* first
  commit — no retraction debt.
* Known deferred items: F2 counterexample (rung 1); GR↔Trace wiring
  (rung 1); `Interface.strict` currently unused (rung 2 or delete);
  LICENSE text TODO (copy from palimpsest-license, do not improvise).

== Next action

Rung 3 (see ROADMAP.adoc): tropical/estate integration. Import the
estate's fully proved `TropicalAdapterPath.lean`
(tropical-resource-typing) behind the `Marches/Tropical.lean` seam —
vendor or depend on it, do NOT paraphrase. Path provenance as graded
accumulation (echo-types bridge). Obtain the real file; never
reconstruct from memory. One rung only.
